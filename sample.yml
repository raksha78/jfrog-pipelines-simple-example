pipelines:
  - name: basic1
    steps:
      - name: basic
        type: Bash
        configuration:
          inputResources:
          runtime:
            type: host
        execution:
          onExecute:
            - echo lol
