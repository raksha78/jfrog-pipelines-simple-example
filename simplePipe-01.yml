pipelines:
- name: simplePipe-01
  steps:    
    - name: simplePipe-01
      type: Matrix
      stepMode: Bash
      stepletMultipliers:
        environmentVariables:             
          - animal:    dog                
            mineral:   copper
             
      execution:
        onExecute:
          - echo "Variation animal = ${animal}, mineral = ${mineral}, vegetable = ${vegetable}"
